{
  "name": "backend",
  "version": "0.0.1",
  "description": "NestJS backend for my data portal",
  "scripts": {
    "start": "nest start --watch",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/main",
    "build": "nest build",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "@nestjs/common": "^9.0.0",
    "@nestjs/core": "^9.0.0",
    "@nestjs/passport": "^9.0.0",
    "@nestjs/jwt": "^9.0.0",
    "passport": "^0.6.0",
    "passport-jwt": "^4.0.0",
    "otplib": "^12.0.0"
  },
  "devDependencies": {
    "@nestjs/cli": "^9.0.0",
    "@nestjs/schematics": "^9.0.0",
    "eslint": "^8.0.0",
    "typescript": "^4.0.0"
  }
}

